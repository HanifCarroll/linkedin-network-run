# Review UI Fixtures

The review UI currently uses stub read models because the app-owned read models
for Threads 3-5 are not implemented yet. Tests assert the route, template, and
safety contracts against those stub rows.
